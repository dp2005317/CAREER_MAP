rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write for now for prototyping
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
